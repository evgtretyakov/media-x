# Media-X: Современная Интерактивная Веб-Презентация

![Media-X Logo](https://via.placeholder.com/150x150/667eea/ffffff?text=MX) <!-- Placeholder для логотипа -->

**Media-X** — это современная, адаптивная и интерактивная веб-презентация с богатым набором функций для создания эффектных демонстраций. Проект сочетает в себе элегантный дизайн, плавные анимации и многоуровневую систему навигации.

## 🚀 Быстрый старт

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ваш-username/media-x.git
   cd media-x
   ```

2. **Запустите презентацию:**
   - **ПРОСТО ОТКРОЙТЕ ФАЙЛ** `presentation/index.html` в браузере
   - Проект работает напрямую без сервера
   - start presentation/index.html

3. **Начните навигацию:**
   - Используйте кнопки навигации
   - Нажимайте стрелки на клавиатуре
   - Свайпайте на мобильных устройствах
   - Кликайте по правой/левой частям слайда

## ✨ Ключевые особенности

### 🎯 Многоуровневая навигация
- **Кнопки навигации**: Интуитивные круглые кнопки с анимацией hover
- **Клавиатура**: Полная поддержка клавиш (← →, Пробел, PageUp/PageDown, Home/End, цифры 1-4)
- **Тач-жесты**: Свайп влево/вправо на мобильных устройствах
- **Клик по областям**: Клик в правой части → следующий слайд, в левой → предыдущий
- **Прокрутка мышью**: Колесико мыши для навигации вперед/назад

### 🎨 Визуальные элементы
- **Прогресс-бар**: Анимированная полоса прогресса с градиентом
- **Счетчик слайдов**: Текущий/общий счетчик с красивым оформлением
- **Плавные анимации**: CSS и jQuery анимации с кастомными easing функциями
- **Градиентный дизайн**: Современные градиенты и тени
- **Адаптивность**: Полная поддержка мобильных устройств
- **Темная тема**: Автоматическое переключение согласно системным настройкам

### 🛠 Технологический стек
- **HTML5**: Семантическая разметка
- **CSS3**: Переменные, Flexbox, Grid, Медиа-запросы
- **JavaScript ES6+**: Современный JavaScript с классами и модульной организацией
- **Vanilla JS**: Нативный JavaScript без jQuery зависимостей
- **CSS-анимации**: Производительные нативные анимации

## 📁 Структура проекта

```
media-x/
├── presentation/
│   ├── index.html      # Основной HTML файл
│   ├── style.css       # Стили и анимации
│   └── script.js       # Логика и навигация
├── LICENSE            # MIT лицензия
└── README.md          # Эта документация
```

## 🎮 Руководство по использованию

### Основная навигация

**Клавиатура:**
- `→` или `Пробел` или `PageDown` → Следующий слайд
- `←` или `PageUp` → Предыдущий слайд  
- `Home` → Первый слайд
- `End` → Последний слайд
- `1-4` → Переход к конкретному слайду

**Мышь:**
- Клик по кнопкам навигации
- Клик в правой части слайда (70%) → Вперед
- Клик в левой части слайда (30%) → Назад
- Колесико вниз → Вперед
- Колесико вверх → Назад

**Сенсорные устройства:**
- Свайп влево → Следующий слайд
- Свайп вправо → Предыдущий слайд

### Добавление новых слайдов

1. **Скопируйте структуру существующего слайда:**
   ```html
   <div class="slide">
       <h2>Заголовок слайда</h2>
       <p>Текстовое содержимое</p>
       <!-- Дополнительный контент -->
   </div>
   ```

2. **Разместите перед закрывающим тегом `</div>` слайдов:**
   ```html
   <!-- Ваш новый слайд -->
   <div class="slide">
       <h2>Новый слайд</h2>
       <p>Описание нового слайда</p>
   </div>
   </div> <!-- Закрывающий тег slides-container -->
   ```

3. **Обновите общее количество слайдов** (автоматически определяется скриптом)

### Кастомизация стилей

**Цветовая схема:** Измените CSS переменные в `:root`:
```css
:root {
    --primary-gradient: linear-gradient(135deg, #ваш-цвет-1 0%, #ваш-цвет-2 100%);
    --secondary-gradient: linear-gradient(135deg, #ваш-цвет-3 0%, #ваш-цвет-4 100%);
    --accent-color: #ваш-акцентный-цвет;
    /* ... другие переменные */
}
```

**Анимации:** Настройте timing functions и durations в CSS и JavaScript

## 🔧 API документация

Презентация предоставляет публичный API через глобальный объект `window.presentation`:

### Методы управления

```javascript
// Перейти к следующему слайду
window.presentation.next();

// Перейти к предыдущему слайду  
window.presentation.prev();

// Перейти к конкретному слайду (0-based индекс)
window.presentation.goTo(slideIndex);

// Перейти к первому слайду
window.presentation.first();

// Перейти к последнему слайду
window.presentation.last();
```

### Методы получения информации

```javascript
// Получить текущий слайд (0-based)
const currentSlide = window.presentation.getCurrent();

// Получить общее количество слайдов
const totalSlides = window.presentation.getTotal();

// Проверить, выполняется ли анимация
const isAnimating = window.presentation.isAnimating();
```

### Пример использования API

```javascript
// Автоматическое пролистывание каждые 5 секунд
setInterval(() => {
    if (!window.presentation.isAnimating()) {
        window.presentation.next();
    }
}, 5000);

// Переход к слайду по клику на внешний элемент
document.getElementById('goto-slide-3').addEventListener('click', () => {
    window.presentation.goTo(2); // 0-based индекс
});
```

## 🌐 Браузерная совместимость

### Полностью поддерживается:
- ✅ Chrome 60+
- ✅ Firefox 55+
- ✅ Safari 12+
- ✅ Edge 79+
- ✅ Mobile Safari (iOS 12+)
- ✅ Chrome Mobile (Android 8+)

### Частично поддерживается:
- ⚠️ IE 11 (ограниченная функциональность)
- ⚠️ Старые версии браузеров (без CSS переменных)

### Требования:
- Включенный JavaScript
- Поддержка CSS Flexbox
- Поддержка CSS transitions/animations

## 🎨 Кастомизация и расширение

### Добавление собственных анимаций

**CSS анимации:**
```css
@keyframes your-custom-animation {
    from { opacity: 0; transform: scale(0.8); }
    to { opacity: 1; transform: scale(1); }
}

.slide.your-custom-class {
    animation: your-custom-animation 0.8s ease-out;
}
```

**JavaScript события:**
```javascript
// Слушатель события смены слайда
$(document).on('slideChanged', function(e, data) {
    console.log('Слайд изменен:', data.currentSlide, '→', data.newSlide);
});

// Слушатель завершения анимации
$(document).on('animationComplete', function() {
    console.log('Анимация завершена');
});
```

### Интеграция с другими библиотеками

**React компонент:**
```jsx
import { useEffect } from 'react';

function PresentationController() {
    useEffect(() => {
        // Интеграция с React
        const handleSlideChange = () => {
            // Обновление состояния React
        };
        
        $(document).on('slideChanged', handleSlideChange);
        
        return () => {
            $(document).off('slideChanged', handleSlideChange);
        };
    }, []);
    
    return <button onClick={() => window.presentation.next()}>Далее</button>;
}
```

## 🐛 Отладка и разработка

### Включение логов навигации
В `script.js` можно добавить `console.log` для отладки (в продакшене логи отключены):
```javascript
// Пример отладочного логирования
console.log('Навигация по презентации');
```

### Проверка состояний
```javascript
// В консоли браузера
console.log('Текущий слайд:', window.presentation.getCurrent());
console.log('Всего слайдов:', window.presentation.getTotal());
console.log('Анимация выполняется:', window.presentation.isAnimating());
```

## 📄 Лицензия

Этот проект распространяется под **MIT License** - смотрите файл [LICENSE](LICENSE) для подробностей.

**Copyright © 2025 Evgeny Tretyakov**

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Чтобы внести свой вклад:

1. Форкните репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка и контакты

Если у вас есть вопросы или предложения:

- **Создайте Issue** на GitHub
- **Напишите email**: ваш-email@example.com
- **Присоединяйтесь к обсуждению** в Discussions

## 🔮 Планы на будущее

- [ ] Расширенные возможности анимации
- [ ] Поддержка 3D трансформаций

---

**Media-X** — сделайте ваши презентации незабываемыми! 🚀

*Последнее обновление: Сентябрь 2025*
